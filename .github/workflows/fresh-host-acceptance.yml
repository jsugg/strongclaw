name: Fresh Host Acceptance

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      enable_package_cache:
        description: Restore uv and npm download caches for bootstrap.
        required: true
        type: boolean
        default: false
      enable_homebrew_cache:
        description: Restore the hosted macOS Homebrew download cache.
        required: true
        type: boolean
        default: false
      enable_docker_image_cache:
        description: Restore the hosted macOS immutable Docker image cache.
        required: true
        type: boolean
        default: false

concurrency:
  group: fresh-host-acceptance-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  DEFAULT_PACKAGE_CACHE_ENABLED: "false"
  DEFAULT_HOMEBREW_CACHE_ENABLED: "false"
  DEFAULT_DOCKER_IMAGE_CACHE_ENABLED: "false"
  FRESH_HOST_DOCKER_PULL_PARALLELISM: "3"
  MACOS_COLIMA_VERSION: "v0.10.1"
  MACOS_LIMA_VERSION: "v2.0.3"
  MACOS_COLIMA_DISK_GIB: "20"
  FRESH_HOST_CACHE_ROOT: ${{ github.workspace }}/.github-cache
  UV_CACHE_DIR: ${{ github.workspace }}/.github-cache/uv
  npm_config_cache: ${{ github.workspace }}/.github-cache/npm
  npm_config_prefer_offline: "true"
  HOMEBREW_CACHE: ${{ github.workspace }}/.github-cache/homebrew

jobs:
  linux-fresh-host:
    name: Linux Fresh Host
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: "22.22.1"
          package-manager-cache: false
      - name: Restore package download caches
        id: linux_package_cache
        if: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_package_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_PACKAGE_CACHE_ENABLED == 'true') }}
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: |
            .github-cache/npm
            .github-cache/uv
          key: fresh-host-packages-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('uv.lock', 'src/clawops/strongclaw_bootstrap.py', 'platform/plugins/memory-lancedb-pro/package-lock.json') }}
          restore-keys: |
            fresh-host-packages-${{ runner.os }}-${{ runner.arch }}-
      - name: Prepare Linux fresh-host environment
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p "${RUNNER_TEMP}/fresh-host-reports"
          : > "${RUNNER_TEMP}/fresh-host-linux-phases.tsv"
          mkdir -p "${FRESH_HOST_CACHE_ROOT}/npm" "${FRESH_HOST_CACHE_ROOT}/uv"
          {
            echo "TMP_ROOT=${RUNNER_TEMP}/strongclaw-linux-host"
            echo "STRONGCLAW_APP_HOME=${RUNNER_TEMP}/strongclaw-linux-host/home"
            echo "STRONGCLAW_XDG_RUNTIME_DIR=${RUNNER_TEMP}/strongclaw-linux-host/xdg-runtime"
            echo "FRESH_HOST_PHASES_FILE=${RUNNER_TEMP}/fresh-host-linux-phases.tsv"
            echo "FRESH_HOST_REPORT_DIR=${RUNNER_TEMP}/fresh-host-reports/linux"
            echo "FRESH_HOST_REPORT_JSON=${RUNNER_TEMP}/fresh-host-reports/linux/report.json"
          } >> "${GITHUB_ENV}"
      - name: Bootstrap Linux host
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export XDG_RUNTIME_DIR="${STRONGCLAW_XDG_RUNTIME_DIR}"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export PATH="${HOME}/.local/bin:${PATH}"
          export PYTHONPATH="src"
          mkdir -p "${HOME}" "${XDG_RUNTIME_DIR}"
          chmod 700 "${XDG_RUNTIME_DIR}"
          python3 -m clawops bootstrap --repo-root . --home-dir "${HOME}" --profile openclaw-default
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Bootstrap Linux host\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Setup Linux host
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export XDG_RUNTIME_DIR="${STRONGCLAW_XDG_RUNTIME_DIR}"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export PATH="${HOME}/.local/bin:${PATH}"
          export PYTHONPATH="src"
          .venv/bin/python -m clawops setup --repo-root . --home-dir "${HOME}" --profile openclaw-default --non-interactive --no-verify --no-activate-services
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Setup Linux host\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Verify Linux rendered units
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          test -f "${HOME}/.config/systemd/user/openclaw-gateway.service"
          test -f "${HOME}/.config/systemd/user/openclaw-sidecars.service"
          test -f "${HOME}/.config/systemd/user/openclaw-browserlab.service"
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Verify Linux rendered units\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Exercise Linux repo-local sidecars
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export XDG_RUNTIME_DIR="${STRONGCLAW_XDG_RUNTIME_DIR}"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export PATH="${HOME}/.local/bin:${PATH}"
          export PYTHONPATH="src"
          .venv/bin/python -m clawops ops --repo-root . sidecars up --repo-local-state
          .venv/bin/python -m clawops ops --repo-root . sidecars down --repo-local-state
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Exercise Linux repo-local sidecars\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Exercise Linux repo-local browser-lab
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export XDG_RUNTIME_DIR="${STRONGCLAW_XDG_RUNTIME_DIR}"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export PATH="${HOME}/.local/bin:${PATH}"
          export PYTHONPATH="src"
          .venv/bin/python -m clawops ops --repo-root . browser-lab up --repo-local-state
          .venv/bin/python -m clawops ops --repo-root . browser-lab down --repo-local-state
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Exercise Linux repo-local browser-lab\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Collect Linux diagnostics
        if: always()
        shell: bash
        run: |
          set +e
          mkdir -p "${FRESH_HOST_REPORT_DIR}"
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export XDG_RUNTIME_DIR="${STRONGCLAW_XDG_RUNTIME_DIR}"
          docker info > "${FRESH_HOST_REPORT_DIR}/docker-info.txt" 2>&1 || true
          docker system df > "${FRESH_HOST_REPORT_DIR}/docker-system-df.txt" 2>&1 || true
          docker images --format '{{json .}}' > "${FRESH_HOST_REPORT_DIR}/docker-images.jsonl" 2>&1 || true
      - name: Write Linux summary
        if: always()
        shell: bash
        env:
          FRESH_HOST_JOB_NAME: Linux Fresh Host
          FRESH_HOST_JOB_STATUS: ${{ job.status }}
          FRESH_HOST_RUNTIME_PROVIDER: docker
          FRESH_HOST_RUNNER_LABEL: ubuntu-latest
          FRESH_HOST_PACKAGE_CACHE_ENABLED: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_package_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_PACKAGE_CACHE_ENABLED == 'true') }}
          FRESH_HOST_PACKAGE_CACHE_HIT: ${{ steps.linux_package_cache.outputs.cache-hit == 'true' }}
          FRESH_HOST_HOMEBREW_CACHE_ENABLED: false
        run: |
          python3 - <<'PY'
          from __future__ import annotations

          import json
          import os
          from pathlib import Path

          def format_duration(seconds: float) -> str:
              total = int(round(seconds))
              minutes, remaining = divmod(total, 60)
              return f"{minutes}m {remaining}s"

          phase_path = Path(os.environ["FRESH_HOST_PHASES_FILE"])
          phases: list[dict[str, object]] = []
          if phase_path.exists():
              for raw_line in phase_path.read_text(encoding="utf-8").splitlines():
                  if not raw_line.strip():
                      continue
                  name, seconds_text, status = raw_line.split("\t")
                  phases.append(
                      {
                          "name": name,
                          "seconds": int(seconds_text),
                          "status": status,
                          "duration": format_duration(float(seconds_text)),
                      }
                  )
          report = {
              "jobName": os.environ["FRESH_HOST_JOB_NAME"],
              "jobStatus": os.environ["FRESH_HOST_JOB_STATUS"],
              "runnerLabel": os.environ["FRESH_HOST_RUNNER_LABEL"],
              "runtimeProvider": os.environ["FRESH_HOST_RUNTIME_PROVIDER"],
              "packageCacheEnabled": os.environ["FRESH_HOST_PACKAGE_CACHE_ENABLED"] == "true",
              "homebrewCacheEnabled": False,
              "phases": phases,
              "reportDir": os.environ["FRESH_HOST_REPORT_DIR"],
          }
          report_path = Path(os.environ["FRESH_HOST_REPORT_JSON"])
          report_path.parent.mkdir(parents=True, exist_ok=True)
          report_path.write_text(json.dumps(report, indent=2, sort_keys=True) + "\n", encoding="utf-8")

          summary_path = Path(os.environ["GITHUB_STEP_SUMMARY"])
          with summary_path.open("a", encoding="utf-8") as handle:
              handle.write(f"## {report['jobName']}\n\n")
              handle.write("| Field | Value |\n| --- | --- |\n")
              handle.write(f"| Status | {report['jobStatus']} |\n")
              handle.write(f"| Runner | {report['runnerLabel']} |\n")
              handle.write(f"| Runtime | {report['runtimeProvider']} |\n")
              handle.write(f"| Package cache | {report['packageCacheEnabled']} |\n")
              handle.write(f"| Package cache hit | {os.environ['FRESH_HOST_PACKAGE_CACHE_HIT'] == 'true'} |\n")
              handle.write("| Homebrew cache | false |\n")
              handle.write("\n")
              if phases:
                  handle.write("| Phase | Status | Duration |\n| --- | --- | --- |\n")
                  for phase in phases:
                      handle.write(
                          f"| {phase['name']} | {phase['status']} | {phase['duration']} |\n"
                      )
                  total_seconds = sum(int(phase["seconds"]) for phase in phases)
                  handle.write(f"\nKnown phase total: {format_duration(float(total_seconds))}\n\n")
              else:
                  handle.write("No phase timings were recorded.\n\n")
              handle.write(f"Diagnostics artifact root: `{report['reportDir']}`\n\n")
          PY
      - name: Upload Linux host artifacts
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: linux-fresh-host-artifacts
          path: |
            ${{ runner.temp }}/strongclaw-linux-host/home/.openclaw
            ${{ runner.temp }}/strongclaw-linux-host/home/.config/systemd/user
            ${{ runner.temp }}/fresh-host-reports/linux
          include-hidden-files: true

  macos-fresh-host:
    name: macOS Fresh Host
    # GitHub-hosted arm64 macOS runners do not support nested virtualization.
    runs-on: macos-15-intel
    timeout-minutes: 90
    env:
      STRONGCLAW_COMPOSE_VARIANT: ci-hosted-macos
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.12"
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: "22.22.1"
          package-manager-cache: false
      - name: Restore package download caches
        id: macos_package_cache
        if: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_package_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_PACKAGE_CACHE_ENABLED == 'true') }}
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: |
            .github-cache/npm
            .github-cache/uv
          key: fresh-host-packages-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('uv.lock', 'src/clawops/strongclaw_bootstrap.py', 'platform/plugins/memory-lancedb-pro/package-lock.json') }}
          restore-keys: |
            fresh-host-packages-${{ runner.os }}-${{ runner.arch }}-
      - name: Restore hosted macOS Homebrew download cache
        id: macos_homebrew_cache
        if: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_homebrew_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_HOMEBREW_CACHE_ENABLED == 'true') }}
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: ${{ env.HOMEBREW_CACHE }}
          key: fresh-host-homebrew-${{ runner.os }}-${{ runner.arch }}-docker-tooling
          restore-keys: |
            fresh-host-homebrew-${{ runner.os }}-${{ runner.arch }}-
      - name: Restore hosted macOS Docker image cache
        id: macos_docker_image_cache
        if: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_docker_image_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_DOCKER_IMAGE_CACHE_ENABLED == 'true') }}
        uses: actions/cache@0400d5f644dc74513175e3cd8d07132dd4860809 # v4.2.4
        with:
          path: .github-cache/docker-images
          key: fresh-host-docker-images-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('platform/compose/docker-compose.aux-stack.ci-hosted-macos.yaml', 'platform/compose/docker-compose.browser-lab.ci-hosted-macos.yaml') }}
          restore-keys: |
            fresh-host-docker-images-${{ runner.os }}-${{ runner.arch }}-
      - name: Prepare macOS fresh-host environment
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p "${RUNNER_TEMP}/fresh-host-reports"
          : > "${RUNNER_TEMP}/fresh-host-macos-phases.tsv"
          mkdir -p \
            "${FRESH_HOST_CACHE_ROOT}/docker-images" \
            "${FRESH_HOST_CACHE_ROOT}/homebrew" \
            "${FRESH_HOST_CACHE_ROOT}/npm" \
            "${FRESH_HOST_CACHE_ROOT}/uv"
          host_project_name="$(PYTHONPATH=src python3 - <<'PY'
          import os
          from pathlib import Path
          from clawops.strongclaw_compose import compose_project_name

          tmp_root = Path(os.environ["RUNNER_TEMP"]) / "strongclaw-macos-host" / "home"
          state_dir = tmp_root / ".openclaw" / "compose"
          print(
              compose_project_name(
                  compose_name="docker-compose.aux-stack.yaml",
                  state_dir=state_dir,
                  repo_local_state=False,
              )
              or ""
          )
          PY
          )"
          {
            echo "RUNNER_HOME=${HOME}"
            echo "TMP_ROOT=${RUNNER_TEMP}/strongclaw-macos-host"
            echo "STRONGCLAW_APP_HOME=${RUNNER_TEMP}/strongclaw-macos-host/home"
            echo "FRESH_HOST_PHASES_FILE=${RUNNER_TEMP}/fresh-host-macos-phases.tsv"
            echo "FRESH_HOST_REPORT_DIR=${RUNNER_TEMP}/fresh-host-reports/macos"
            echo "FRESH_HOST_REPORT_JSON=${RUNNER_TEMP}/fresh-host-reports/macos/report.json"
            echo "FRESH_HOST_RUNTIME_PROVIDER=colima"
            echo "FRESH_HOST_DOCKER_IMAGE_CACHE_DIR=${GITHUB_WORKSPACE}/.github-cache/docker-images"
            echo "FRESH_HOST_MACOS_AUX_COMPOSE_FILE=${GITHUB_WORKSPACE}/platform/compose/docker-compose.aux-stack.ci-hosted-macos.yaml"
            echo "FRESH_HOST_MACOS_BROWSER_LAB_COMPOSE_FILE=${GITHUB_WORKSPACE}/platform/compose/docker-compose.browser-lab.ci-hosted-macos.yaml"
            echo "FRESH_HOST_MACOS_HOST_PROJECT=${host_project_name}"
          } >> "${GITHUB_ENV}"
      - name: Install Docker tooling for hosted macOS
        shell: bash
        env:
          HOMEBREW_NO_AUTO_UPDATE: "1"
          HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK: "1"
          HOMEBREW_NO_INSTALL_CLEANUP: "1"
          HOMEBREW_NO_INSTALL_UPGRADE: "1"
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          arch_name="$(uname -m)"
          rc=0
          if [[ "${arch_name}" != "x86_64" ]]; then
            echo "GitHub-hosted arm64 macOS runners do not support nested virtualization; Fresh Host Acceptance requires macos-15-intel." >&2
            rc=1
          else
            sudo mkdir -p /usr/local/libexec
            sudo chown "$(id -u):$(id -g)" /usr/local/libexec
            curl -fsSL "https://github.com/lima-vm/lima/releases/download/${MACOS_LIMA_VERSION}/lima-${MACOS_LIMA_VERSION#v}-Darwin-${arch_name}.tar.gz" | sudo tar xz -C /usr/local
            curl -fsSL "https://github.com/abiosoft/colima/releases/download/${MACOS_COLIMA_VERSION}/colima-Darwin-${arch_name}" -o /tmp/colima
            sudo install -m 0755 /tmp/colima /usr/local/bin/colima
            brew install docker docker-compose
            mkdir -p "${RUNNER_HOME}/.docker/cli-plugins"
            ln -sf "$(brew --prefix docker-compose)/bin/docker-compose" "${RUNNER_HOME}/.docker/cli-plugins/docker-compose"
            export DOCKER_CONFIG="${RUNNER_HOME}/.docker"
            export DOCKER_HOST="unix://${RUNNER_HOME}/.colima/default/docker.sock"
            host_cpu_count="$(sysctl -n hw.ncpu)"
            host_memory_bytes="$(sysctl -n hw.memsize)"
            host_memory_gib="$(( host_memory_bytes / 1073741824 ))"
            if (( host_memory_gib < 1 )); then
              host_memory_gib=1
            fi
            colima_memory_gib="$(( host_memory_gib - 3 ))"
            if (( colima_memory_gib < 6 )); then
              colima_memory_gib=6
            fi
            if (( colima_memory_gib > 10 )); then
              colima_memory_gib=10
            fi
            {
              echo "DOCKER_CONFIG=${DOCKER_CONFIG}"
              echo "DOCKER_HOST=${DOCKER_HOST}"
              echo "FRESH_HOST_HOST_CPU_COUNT=${host_cpu_count}"
              echo "FRESH_HOST_HOST_MEMORY_GIB=${host_memory_gib}"
              echo "FRESH_HOST_COLIMA_CPU_COUNT=${host_cpu_count}"
              echo "FRESH_HOST_COLIMA_MEMORY_GIB=${colima_memory_gib}"
            } >> "${GITHUB_ENV}"
            colima start --cpu "${host_cpu_count}" --memory "${colima_memory_gib}" --disk "${MACOS_COLIMA_DISK_GIB}" --arch x86_64 --vm-type=vz
            for _ in $(seq 1 45); do
              if docker info >/dev/null 2>&1; then
                break
              fi
              sleep 2
            done
            docker version
            docker compose version
            docker info
            rc=$?
          fi
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Install Docker tooling for hosted macOS\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Normalize hosted macOS machine name
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          sudo scutil --set ComputerName "openclaw-ci"
          sudo scutil --set LocalHostName "openclaw-ci"
          sudo scutil --set HostName "openclaw-ci"
          scutil --get ComputerName
          scutil --get LocalHostName
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Normalize hosted macOS machine name\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Ensure hosted macOS images
        shell: bash
        env:
          FRESH_HOST_DOCKER_IMAGE_CACHE_ENABLED: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_docker_image_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_DOCKER_IMAGE_CACHE_ENABLED == 'true') }}
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          cache_args=()
          if [[ "${FRESH_HOST_DOCKER_IMAGE_CACHE_ENABLED}" == "true" ]]; then
            cache_args+=(--cache-dir "${FRESH_HOST_DOCKER_IMAGE_CACHE_DIR}")
          fi
          python3 .github/scripts/fresh_host_images.py ensure \
            --parallelism "${FRESH_HOST_DOCKER_PULL_PARALLELISM}" \
            --report-path "${FRESH_HOST_REPORT_DIR}/image-ensure-report.json" \
            "${cache_args[@]}" \
            "${FRESH_HOST_MACOS_AUX_COMPOSE_FILE}" \
            "${FRESH_HOST_MACOS_BROWSER_LAB_COMPOSE_FILE}"
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Ensure hosted macOS images\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Bootstrap macOS host
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export OPENCLAW_MDNS_HOSTNAME="openclaw-ci"
          export STRONGCLAW_COMPOSE_VARIANT="ci-hosted-macos"
          export PATH="${HOME}/.config/varlock/bin:${HOME}/.local/bin:/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin:${PATH}"
          export PYTHONPATH="src"
          mkdir -p "${HOME}"
          python3 -m clawops bootstrap --repo-root . --home-dir "${HOME}" --profile openclaw-default
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Bootstrap macOS host\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Setup macOS host and activate services
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export OPENCLAW_MDNS_HOSTNAME="openclaw-ci"
          export STRONGCLAW_LAUNCHD_SIDECARS_TIMEOUT_SECONDS="2700"
          export STRONGCLAW_REPO_LOCAL_COMPOSE_STATE_DIR="${HOME}/.openclaw/repo-local-compose"
          export STRONGCLAW_COMPOSE_VARIANT="ci-hosted-macos"
          export PATH="${HOME}/.config/varlock/bin:${HOME}/.local/bin:/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin:${PATH}"
          export PYTHONPATH="src"
          mkdir -p "${STRONGCLAW_REPO_LOCAL_COMPOSE_STATE_DIR}"
          .venv/bin/python -m clawops setup --repo-root . --home-dir "${HOME}" --profile openclaw-default --non-interactive --no-verify
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Setup macOS host and activate services\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Verify macOS launchd services
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          launchctl print "gui/$(id -u)/ai.openclaw.gateway" | grep "state = running"
          launchctl print "gui/$(id -u)/ai.openclaw.sidecars" | grep "last exit code = 0"
          test -f "${HOME}/Library/LaunchAgents/ai.openclaw.gateway.plist"
          test -f "${HOME}/Library/LaunchAgents/ai.openclaw.sidecars.plist"
          test -f "${HOME}/Library/LaunchAgents/ai.openclaw.browserlab.plist"
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Verify macOS launchd services\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Exercise macOS repo-local sidecars
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export OPENCLAW_MDNS_HOSTNAME="openclaw-ci"
          export STRONGCLAW_REPO_LOCAL_COMPOSE_STATE_DIR="${HOME}/.openclaw/repo-local-compose"
          export STRONGCLAW_COMPOSE_VARIANT="ci-hosted-macos"
          export PATH="${HOME}/.config/varlock/bin:${HOME}/.local/bin:/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin:${PATH}"
          export PYTHONPATH="src"
          .venv/bin/python -m clawops ops --repo-root . sidecars up --repo-local-state
          .venv/bin/python -m clawops ops --repo-root . sidecars down --repo-local-state
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Exercise macOS repo-local sidecars\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Exercise macOS repo-local browser-lab
        shell: bash
        run: |
          set -euo pipefail
          SECONDS=0
          set +e
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export OPENCLAW_MDNS_HOSTNAME="openclaw-ci"
          export STRONGCLAW_REPO_LOCAL_COMPOSE_STATE_DIR="${HOME}/.openclaw/repo-local-compose"
          export STRONGCLAW_COMPOSE_VARIANT="ci-hosted-macos"
          export PATH="${HOME}/.config/varlock/bin:${HOME}/.local/bin:/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin:${PATH}"
          export PYTHONPATH="src"
          .venv/bin/python -m clawops ops --repo-root . browser-lab up --repo-local-state
          .venv/bin/python -m clawops ops --repo-root . browser-lab down --repo-local-state
          rc=$?
          set -e
          status="success"
          if [[ ${rc} -ne 0 ]]; then status="failure"; fi
          printf 'Exercise macOS repo-local browser-lab\t%s\t%s\n' "${SECONDS}" "${status}" >> "${FRESH_HOST_PHASES_FILE}"
          exit "${rc}"
      - name: Dump macOS host logs on failure
        if: failure()
        shell: bash
        run: |
          set +e
          export OPENCLAW_STATE_DIR="${STRONGCLAW_APP_HOME}/.openclaw"
          export STRONGCLAW_COMPOSE_STATE_DIR="${OPENCLAW_STATE_DIR}/compose"
          export STRONGCLAW_COMPOSE_VARIANT="ci-hosted-macos"
          export COMPOSE_PROJECT_NAME="${FRESH_HOST_MACOS_HOST_PROJECT}"
          mkdir -p "${FRESH_HOST_REPORT_DIR}"
          for path in \
            "${STRONGCLAW_APP_HOME}/.openclaw/logs/launchd-gateway.out.log" \
            "${STRONGCLAW_APP_HOME}/.openclaw/logs/launchd-gateway.err.log" \
            "${STRONGCLAW_APP_HOME}/.openclaw/logs/launchd-sidecars.out.log" \
            "${STRONGCLAW_APP_HOME}/.openclaw/logs/launchd-sidecars.err.log"; do
            if [[ -f "${path}" ]]; then
              echo "--- ${path}"
              tail -n 200 "${path}"
            fi
          done
          docker compose -f "${FRESH_HOST_MACOS_AUX_COMPOSE_FILE}" ps > "${FRESH_HOST_REPORT_DIR}/compose-aux-stack-ps.txt" 2>&1 || true
          docker compose -f "${FRESH_HOST_MACOS_AUX_COMPOSE_FILE}" logs --no-color postgres litellm qdrant otel-collector > "${FRESH_HOST_REPORT_DIR}/compose-aux-stack-logs.txt" 2>&1 || true
          launchctl print "gui/$(id -u)/ai.openclaw.gateway" > "${FRESH_HOST_REPORT_DIR}/launchctl-gateway-failure.txt" 2>&1 || true
          launchctl print "gui/$(id -u)/ai.openclaw.sidecars" > "${FRESH_HOST_REPORT_DIR}/launchctl-sidecars-failure.txt" 2>&1 || true
          cat "${FRESH_HOST_REPORT_DIR}/compose-aux-stack-ps.txt" || true
          cat "${FRESH_HOST_REPORT_DIR}/compose-aux-stack-logs.txt" || true
          cat "${FRESH_HOST_REPORT_DIR}/launchctl-gateway-failure.txt" || true
          cat "${FRESH_HOST_REPORT_DIR}/launchctl-sidecars-failure.txt" || true
      - name: Cleanup macOS host services
        if: always()
        shell: bash
        run: |
          set +e
          SECONDS=0
          export HOME="${STRONGCLAW_APP_HOME}"
          export XDG_CONFIG_HOME="${HOME}/.config"
          export OPENCLAW_CONFIG_PROFILE="openclaw-default"
          export OPENCLAW_MODEL_SETUP_MODE="skip"
          export OPENCLAW_MDNS_HOSTNAME="openclaw-ci"
          export STRONGCLAW_REPO_LOCAL_COMPOSE_STATE_DIR="${HOME}/.openclaw/repo-local-compose"
          export STRONGCLAW_COMPOSE_VARIANT="ci-hosted-macos"
          export PATH="${HOME}/.config/varlock/bin:${HOME}/.local/bin:/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin:${PATH}"
          export PYTHONPATH="src"
          launchctl bootout "gui/$(id -u)" "${HOME}/Library/LaunchAgents/ai.openclaw.gateway.plist" || true
          launchctl bootout "gui/$(id -u)" "${HOME}/Library/LaunchAgents/ai.openclaw.sidecars.plist" || true
          launchctl bootout "gui/$(id -u)" "${HOME}/Library/LaunchAgents/ai.openclaw.browserlab.plist" || true
          .venv/bin/python -m clawops ops --repo-root . sidecars down || true
          .venv/bin/python -m clawops ops --repo-root . browser-lab down || true
          printf 'Cleanup macOS host services\t%s\tsuccess\n' "${SECONDS}" >> "${FRESH_HOST_PHASES_FILE}"
      - name: Collect macOS diagnostics
        if: always()
        shell: bash
        run: |
          set +e
          mkdir -p "${FRESH_HOST_REPORT_DIR}"
          if command -v colima >/dev/null 2>&1; then
            colima status > "${FRESH_HOST_REPORT_DIR}/runtime-status.txt" 2>&1 || true
            colima list >> "${FRESH_HOST_REPORT_DIR}/runtime-status.txt" 2>&1 || true
          fi
          docker info > "${FRESH_HOST_REPORT_DIR}/docker-info.txt" 2>&1 || true
          docker system df > "${FRESH_HOST_REPORT_DIR}/docker-system-df.txt" 2>&1 || true
          docker images --format '{{json .}}' > "${FRESH_HOST_REPORT_DIR}/docker-images.jsonl" 2>&1 || true
          launchctl print "gui/$(id -u)/ai.openclaw.gateway" > "${FRESH_HOST_REPORT_DIR}/launchctl-gateway.txt" 2>&1 || true
          launchctl print "gui/$(id -u)/ai.openclaw.sidecars" > "${FRESH_HOST_REPORT_DIR}/launchctl-sidecars.txt" 2>&1 || true
          sysctl -n hw.ncpu > "${FRESH_HOST_REPORT_DIR}/host-cpu-count.txt" 2>&1 || true
          sysctl -n hw.memsize > "${FRESH_HOST_REPORT_DIR}/host-memory-bytes.txt" 2>&1 || true
          du -sh "${RUNNER_HOME}/.colima" > "${FRESH_HOST_REPORT_DIR}/colima-disk-usage.txt" 2>&1 || true
          du -sh "${HOMEBREW_CACHE}" > "${FRESH_HOST_REPORT_DIR}/homebrew-cache-usage.txt" 2>&1 || true
          du -sh "${GITHUB_WORKSPACE}/.github-cache" > "${FRESH_HOST_REPORT_DIR}/workflow-cache-usage.txt" 2>&1 || true
          if [[ -d "${RUNNER_HOME}/.colima/_lima/colima" ]]; then
            mkdir -p "${FRESH_HOST_REPORT_DIR}/colima-logs"
            find "${RUNNER_HOME}/.colima/_lima/colima" -maxdepth 1 -type f -name '*.log' -exec cp {} "${FRESH_HOST_REPORT_DIR}/colima-logs/" \; || true
          fi
      - name: Write macOS summary
        if: always()
        shell: bash
        env:
          FRESH_HOST_JOB_NAME: macOS Fresh Host
          FRESH_HOST_JOB_STATUS: ${{ job.status }}
          FRESH_HOST_RUNNER_LABEL: macos-15-intel
          FRESH_HOST_PACKAGE_CACHE_ENABLED: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_package_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_PACKAGE_CACHE_ENABLED == 'true') }}
          FRESH_HOST_PACKAGE_CACHE_HIT: ${{ steps.macos_package_cache.outputs.cache-hit == 'true' }}
          FRESH_HOST_HOMEBREW_CACHE_ENABLED: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_homebrew_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_HOMEBREW_CACHE_ENABLED == 'true') }}
          FRESH_HOST_HOMEBREW_CACHE_HIT: ${{ steps.macos_homebrew_cache.outputs.cache-hit == 'true' }}
          FRESH_HOST_DOCKER_IMAGE_CACHE_ENABLED: ${{ (github.event_name == 'workflow_dispatch' && inputs.enable_docker_image_cache) || (github.event_name != 'workflow_dispatch' && env.DEFAULT_DOCKER_IMAGE_CACHE_ENABLED == 'true') }}
          FRESH_HOST_DOCKER_IMAGE_CACHE_HIT: ${{ steps.macos_docker_image_cache.outputs.cache-hit == 'true' }}
          FRESH_HOST_DOCKER_PULL_PARALLELISM: ${{ env.FRESH_HOST_DOCKER_PULL_PARALLELISM }}
        run: |
          python3 - <<'PY'
          from __future__ import annotations

          import json
          import os
          from pathlib import Path

          def format_duration(seconds: float) -> str:
              total = int(round(seconds))
              minutes, remaining = divmod(total, 60)
              return f"{minutes}m {remaining}s"

          phase_path = Path(os.environ["FRESH_HOST_PHASES_FILE"])
          image_report_path = Path(os.environ["FRESH_HOST_REPORT_DIR"]) / "image-ensure-report.json"
          phases: list[dict[str, object]] = []
          if phase_path.exists():
              for raw_line in phase_path.read_text(encoding="utf-8").splitlines():
                  if not raw_line.strip():
                      continue
                  name, seconds_text, status = raw_line.split("\t")
                  phases.append(
                      {
                          "name": name,
                          "seconds": int(seconds_text),
                          "status": status,
                          "duration": format_duration(float(seconds_text)),
                      }
                  )
          image_report: dict[str, object] = {}
          if image_report_path.exists():
              image_report = json.loads(image_report_path.read_text(encoding="utf-8"))
          report = {
              "jobName": os.environ["FRESH_HOST_JOB_NAME"],
              "jobStatus": os.environ["FRESH_HOST_JOB_STATUS"],
              "runnerLabel": os.environ["FRESH_HOST_RUNNER_LABEL"],
              "runtimeProvider": os.environ["FRESH_HOST_RUNTIME_PROVIDER"],
              "packageCacheEnabled": os.environ["FRESH_HOST_PACKAGE_CACHE_ENABLED"] == "true",
              "packageCacheHit": os.environ["FRESH_HOST_PACKAGE_CACHE_HIT"] == "true",
              "homebrewCacheEnabled": os.environ["FRESH_HOST_HOMEBREW_CACHE_ENABLED"] == "true",
              "homebrewCacheHit": os.environ["FRESH_HOST_HOMEBREW_CACHE_HIT"] == "true",
              "dockerImageCacheEnabled": os.environ["FRESH_HOST_DOCKER_IMAGE_CACHE_ENABLED"] == "true",
              "dockerImageCacheHit": os.environ["FRESH_HOST_DOCKER_IMAGE_CACHE_HIT"] == "true",
              "dockerPullParallelism": os.environ["FRESH_HOST_DOCKER_PULL_PARALLELISM"],
              "hostCpuCount": os.environ.get("FRESH_HOST_HOST_CPU_COUNT"),
              "hostMemoryGiB": os.environ.get("FRESH_HOST_HOST_MEMORY_GIB"),
              "colimaCpuCount": os.environ.get("FRESH_HOST_COLIMA_CPU_COUNT"),
              "colimaMemoryGiB": os.environ.get("FRESH_HOST_COLIMA_MEMORY_GIB"),
              "imageEnsure": image_report,
              "phases": phases,
              "reportDir": os.environ["FRESH_HOST_REPORT_DIR"],
          }
          report_path = Path(os.environ["FRESH_HOST_REPORT_JSON"])
          report_path.parent.mkdir(parents=True, exist_ok=True)
          report_path.write_text(json.dumps(report, indent=2, sort_keys=True) + "\n", encoding="utf-8")

          summary_path = Path(os.environ["GITHUB_STEP_SUMMARY"])
          with summary_path.open("a", encoding="utf-8") as handle:
              handle.write(f"## {report['jobName']}\n\n")
              handle.write("| Field | Value |\n| --- | --- |\n")
              handle.write(f"| Status | {report['jobStatus']} |\n")
              handle.write(f"| Runner | {report['runnerLabel']} |\n")
              handle.write(f"| Runtime | {report['runtimeProvider']} |\n")
              handle.write(f"| Package cache | {report['packageCacheEnabled']} |\n")
              handle.write(f"| Package cache hit | {report['packageCacheHit']} |\n")
              handle.write(f"| Homebrew cache | {report['homebrewCacheEnabled']} |\n")
              handle.write(f"| Homebrew cache hit | {report['homebrewCacheHit']} |\n")
              handle.write(f"| Docker image cache | {report['dockerImageCacheEnabled']} |\n")
              handle.write(f"| Docker image cache hit | {report['dockerImageCacheHit']} |\n")
              handle.write(f"| Docker pull parallelism | {report['dockerPullParallelism']} |\n")
              if report["hostCpuCount"] and report["hostMemoryGiB"]:
                  handle.write(
                      f"| Host resources | {report['hostCpuCount']} CPU / {report['hostMemoryGiB']} GiB |\n"
                  )
              if report["colimaCpuCount"] and report["colimaMemoryGiB"]:
                  handle.write(
                      f"| Colima resources | {report['colimaCpuCount']} CPU / {report['colimaMemoryGiB']} GiB |\n"
                  )
              handle.write("\n")
              if phases:
                  handle.write("| Phase | Status | Duration |\n| --- | --- | --- |\n")
                  for phase in phases:
                      handle.write(
                          f"| {phase['name']} | {phase['status']} | {phase['duration']} |\n"
                      )
                  total_seconds = sum(int(phase["seconds"]) for phase in phases)
                  handle.write(f"\nKnown phase total: {format_duration(float(total_seconds))}\n\n")
              else:
                  handle.write("No phase timings were recorded.\n\n")
              image_report = report["imageEnsure"]
              if image_report:
                  handle.write("| Image ensure field | Value |\n| --- | --- |\n")
                  handle.write(f"| Cache requested | {image_report.get('cache_requested')} |\n")
                  handle.write(f"| Cache available | {image_report.get('cache_available')} |\n")
                  handle.write(f"| Cache loaded | {image_report.get('cache_loaded')} |\n")
                  handle.write(f"| Cache saved | {image_report.get('cache_saved')} |\n")
                  handle.write(f"| Pulled images | {len(image_report.get('pulled_images', []))} |\n")
                  handle.write("\n")
              handle.write(f"Diagnostics artifact root: `{report['reportDir']}`\n\n")
          PY
      - name: Upload macOS host artifacts
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: macos-fresh-host-artifacts
          path: |
            ${{ runner.temp }}/strongclaw-macos-host/home/.openclaw
            ${{ runner.temp }}/strongclaw-macos-host/home/Library/LaunchAgents
            ${{ runner.temp }}/fresh-host-reports/macos
          include-hidden-files: true
