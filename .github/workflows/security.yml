name: Repository Security Analysis

on:
  pull_request:
  push:
    branches: [main]

permissions:
  actions: read
  contents: read
  pull-requests: write
  security-events: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  run-security-scans:
    name: Run Repository Quality and Runtime Security Scans
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7.6.0
        with:
          version: "0.10.9"
          enable-cache: true
      - run: ./scripts/ci/sync_dev_environment.sh
      - run: ./scripts/ci/run_repository_quality_gate.sh
      - name: Coverage summary
        run: python3 ./scripts/ci/write_coverage_summary.py coverage.xml
      - uses: returntocorp/semgrep-action@713efdd345f3035192eaa63f56867b88e63e4e5d # v1
        with:
          config: security/semgrep/semgrep.yml
      - name: Install gitleaks CLI
        env:
          GITLEAKS_VERSION: "8.28.0"
        run: ./scripts/ci/install_gitleaks.sh
      - name: Run gitleaks
        run: ./scripts/ci/run_gitleaks_scan.sh
      - name: Install syft CLI
        env:
          SYFT_VERSION: "v1.42.2"
        run: ./scripts/ci/install_syft.sh
      - name: Generate SBOM
        run: ./scripts/ci/generate_sbom.sh sbom.spdx.json
      - uses: aquasecurity/trivy-action@57a97c7e7821a5776cebc9bb87c984fa69cba8f1 # 0.35.0
        with:
          cache: true
          scan-type: fs
          scan-ref: .
          trivy-config: security/trivy/trivy.yaml
      - uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        if: always()
        with:
          name: security-artifacts
          path: |
            coverage.xml
            sbom.spdx.json

  run-codeql-analysis:
    name: Run CodeQL Analysis
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          fetch-depth: 0
      - uses: github/codeql-action/init@b1bff81932f5cdfc8695c7752dcee935dcd061c8 # v4
        with:
          config-file: security/codeql/codeql-config.yml
          languages: python,javascript
      - uses: github/codeql-action/analyze@b1bff81932f5cdfc8695c7752dcee935dcd061c8 # v4
