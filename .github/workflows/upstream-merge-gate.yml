name: upstream-merge-gate

on:
  workflow_dispatch:

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
      - run: |
          echo "Use this workflow after merging upstream OpenClaw into your fork."
          echo "Run the security and harness workflows before enabling the new build."
